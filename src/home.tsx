import React from "react";
import {Button} from 'react-bootstrap'

const Home : React.FC = () => {
    return (<div>
        <Button  variant="primary" size="lg" type="submit">ADD Complaint</Button>
        </div>
)}

export default Home;